apiVersion: v1
kind: Service
metadata:
  name: company
spec:
  selector:
    app: company
  ports:
    - port: 80 #cluster port within
      #service listen on 80, routes to 81 on container
      targetPort: 8081 #pod port
  type: LoadBalancer #for external access