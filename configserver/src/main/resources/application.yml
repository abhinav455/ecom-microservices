spring:
  application:
    name: configserver

#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/abhinav455/app-configuration.git
#          username: ${GIT_USER}
#          password: ${GIT_TOKEN}


#  profiles:
#    active: jdbc
#  datasource:
#    url: jdbc:mysql://localhost:3306/configdb
#    username: ${USER}
#    password: ${PASSWORD}
#    driver-class-name: com.mysql.cj.jdbc.Driver
#
#  cloud:
#    config:
#      server:
#        jdbc:
#          sql: "SELECT PROP_KEY, PROP_VALUE from PROPERTIES where APPLICATION=? and PROFILE=? and LABEL=?"
#          default-label: main
#


  profiles: #get config from filesystem
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/abhinav.bhattacharje/springboot_proj/ecom_microservices/configserver/src/main/resources/config
                #classpath:/config


  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    virtual-host: ${RABBITMQ_VHOST:guest}
    connection-timeout: 30000


management:
  endpoints:
    web:
      exposure:
        include: "*"



server:
  port: 8898

encrypt:
 # key: "0E329232EA6D0D73"
 keystore:
   location: config-server.jks
   password: mypass
   alias: config-server-key